{
    "$schema": "http://json-schema.org/draft-03/schema",
    "type": "object",
    "extends" : {
        "$ref" : "reply.json"
    },
    "properties": {
        "status": {
            "type": "string",
            "enum": ["complete", "incomplete", "invalid", "unknown"]
        },
        "indent": {
            "type": "string"
        }
    }
}
